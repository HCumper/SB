void foo(param1%,param2%) ( void param1%, void param2%)    
    {    
     

 

 
}

void print_table     
    {    
     

 float pos;

 finished = DATE-now;
 for (pos = n-4; pos <= n; pos += 1 {
}
 
}


 for (n = 1; n <= 10; n += 1 {
continue;

 bb = 22;
}
 avar = 9 + 3 * (4 - 1) - 5 / 6;
 var2 = -3;
 gg = 11;
string astring[,];
 astring = "hhh";
 sorts = 0;
 partitions = 0;
float a[];
float b[,,];
 shortlist = 9;
 for (lo = 1; lo <= n; lo += 1 {
 b = a(lo);
}
 now = DATE;
 b = 10001;
 for (lo = 1; lo <= n; lo += 1 {
 b = a(lo);
}
 sorts = 0;
 partitions = 0;
 now = DATE;void check     
    {    
     

 

 for (loop = 1; loop <= n-1; loop += 1 {
}
 
}


 shouldbeglobal = 104;void set_up_tabLe     
    {    
     

 float entry;

 for (entry = 1; entry <= n; entry += 1 {
 a = RND(1TO10000);
}
 now = DATE;
 
}

void foo(param1%,param2%) ( void param1%, void param2%)    
    {    
     

 

 
}

void print_table     
    {    
     

 float pos;

 finished = DATE-now;
 for (pos = n-4; pos <= n; pos += 1 {
}
 
}

void quicksort(l,r) ( int l, int r)    
    {    
     int i, j, pivot;

 

 i = l;
 j = r;
 pivot = b(i);
 i = i+1;
 j = j-1;
 i = i+1;
 j = j-1;
 
}

void swap(this,that) ( int this, int that)    
    {    
     int t;

 

 t = this;
 this = that;
 that = t;
 
}

void insertionsort(first,last) ( string first, string last)    
    {    
     
string item, p, temporary;
 

 for (item = first+1; item <= last; item += 1 {
 p = item;
 temporary = b(p);
 b = b(p-1);
 p = p-1;
 b = temporary;
}
 
}

void QUICKSORT1(bottom,top) ( float bottom, float top)    
    {    
     

 float high, low, ptr, sort_loop;

 low = bottom;
 high = top;
 ptr = bottom;
 temp = b(low);
 b = b(high);
 b = temp;
 low = low+1;
 ptr = high;
 high = high-1;
 ptr = low;
 high = high-1;
 low = low+1;
 
}

void QUICKSORT2(lower,upper) ( float lower, float upper)    
    {    
     

 float part_elem;

 sorts = sorts+1;
 part_elem = upper+1;
 
}

void QS2_PARTITION(i,j) ( int i, int j)    
    {    
     

 float dec_j, exchange, inc_k, k, loop, temp;

 temp = b((i+j)/2);
 k = i;
 partitions = partitions+1;
 partitions = partitions+1;
 k = k+1;
 j = j-1;
 b = b(j);
 b = temp;
 
}


************************************************
